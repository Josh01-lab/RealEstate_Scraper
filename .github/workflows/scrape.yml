name: Minimal Dispatch Tester

on:
  workflow_dispatch:

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a message
        run: echo "Hello World! If you see this, the trigger is working."
